import React, { ReactNode } from 'react';
import PHome from './pages/PHome';

const App: React.FC<ReactNode> = () => {
  return (
    <div className="App">
      <PHome />
    </div>
  );
};

export default App;
