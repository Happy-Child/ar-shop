import React, { ReactNode } from 'react';
import MPageTitle from '../ui/molecules/MPageTitle';

const PNotFound: React.FC<ReactNode> = () => {
  return <MPageTitle variant="h3">404</MPageTitle>;
};

export default PNotFound;
