import React, { ReactNode } from 'react';
import TDefault from '../ui/templates/TDefault';

const PCategories: React.FC<ReactNode> = () => {
  return <TDefault>PCategories</TDefault>;
};

export default PCategories;
